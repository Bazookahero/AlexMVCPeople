@model List<Person>;
@{
    ViewData["Title"] = "PeopleIndex";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<a asp-controller="People"asp-action="NewPerson">create new person</a>
<form asp-action="Search" asp-controller="People">
    <input id="search" type="text" placeholder="search" name="search" />
    <button type="submit">Search</button>
</form>



@if (Model != null)
{
    <h3>List of People:</h3>
    foreach (Person p in Model)
    {
@*        <div class="list-group">
            <button class="list-group-item list-group-item-action active">
                <li >@p.FirstName
                     @p.LastName
                </li>
            </button>
            @Html.ActionLink("Details","PeopleDetails","People", new {id = p.Id}) @Html.ActionLink("Delete","Delete","People", new {id = p.Id})
            
        </div>*@
        <partial name="/Views/Shared/_PeopleIndexPartial.cshtml"model="p"></partial>
    }
}


